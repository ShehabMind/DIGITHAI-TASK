import React from "react";
import {SafeAreaView, Text, StyleSheet} from "react-native";

const SettingView: React.FC<any> = ({item, index}) => {
  return (
    <SafeAreaView>
      <Text style={{fontSize: 30}}> SettingView</Text>
    </SafeAreaView>
  );
};

export default SettingView;

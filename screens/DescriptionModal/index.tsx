/* eslint-disable prettier/prettier */
import React from "react";
import DescriptionModalView from "./view";

export const DescriptionModalScreen = (props: any) => {
  return <DescriptionModalView {...props} />;
};

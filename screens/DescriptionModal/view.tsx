import React from "react";
import {SafeAreaView, Text, StyleSheet} from "react-native";

const DescriptionModalView: React.FC<any> = ({item, index}) => {
  return (
    <SafeAreaView>
      <Text style={{fontSize: 30}}> DescriptionModalView </Text>
    </SafeAreaView>
  );
};

export default DescriptionModalView;
